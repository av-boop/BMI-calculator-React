import React from 'react'

function Title({titleText,titleclassText}) {
    return (
        
            <p className={titleclassText}>{titleText}</p>
        
    )
}

export default Title

